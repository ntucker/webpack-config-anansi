{
  "name": "@anansi/webpack-config",
  "version": "0.0.1",
  "description": "Webpack config",
  "main": "lib/index.js",
  "scripts": {
    "build:lib": "babel src --out-dir lib --source-maps inline",
    "build": "npm run build:lib",
    "clean": "rimraf lib",
    "prepare": "npm run clean && npm run build",
    "lint": "eslint js"
  },
  "files": [
    "lib",
    "package.json"
  ],
  "keywords": [
    "anansi",
    "webpack"
  ],
  "author": "Nathaniel Tucker",
  "license": "BSD",
  "devDependencies": {
    "@anansi/babel-preset": "^0.4.4",
    "@anansi/eslint-config": "^0.4.0",
    "@babel/cli": "7.0.0",
    "@babel/core": "^7.0.0",
    "better-npm-run": "^0.1.1",
    "debug": "^4.0.1",
    "eslint": "^5.5.0",
    "nsp": "^3.2.1",
    "rimraf": "^2.6.2"
  }
}
